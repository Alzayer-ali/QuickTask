<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:name="com.example.TaskApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskManager">

        <!-- Main Dashboard Activity -->
        <activity
            android:name="com.example.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TaskManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Quick Capture Floating Dialog Activity (Lock Screen Capable) -->
        <activity
            android:name="com.example.QuickCaptureActivity"
            android:exported="true"
            android:label="@string/quick_capture_title"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:windowSoftInputMode="stateVisible|adjustResize"
            android:theme="@style/Theme.TaskManager.QuickCaptureDialog">
            <intent-filter>
                <action android:name="com.example.ACTION_QUICK_CAPTURE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Dedicated Activity to trigger Quick Task Notification -->
        <activity
            android:name="com.example.SendTaskNotificationActivity"
            android:exported="true"
            android:taskAffinity="com.example.notification.trigger"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.TaskManager.NoDisplay"
            android:noHistory="true"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="com.example.ACTION_SEND_TASK_NOTIFICATION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Quick Task Notification Receiver for direct text reply -->
        <receiver
            android:name="com.example.notification.TaskNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.notification.ACTION_ADD_TASK" />
            </intent-filter>
        </receiver>

        <!-- Task Due Date & Time Reminder Receiver -->
        <receiver
            android:name="com.example.notification.TaskReminderReceiver"
            android:exported="false" />

        <!-- Task Reminder Mark Done Action Receiver -->
        <receiver
            android:name="com.example.notification.TaskReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.notification.ACTION_MARK_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- Boot Completed Receiver to Reschedule Reminders -->
        <receiver
            android:name="com.example.notification.BootCompletedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Widget 1: Quick Bar Widget -->
        <receiver
            android:name="com.example.widget.QuickBarAppWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_bar_info" />
        </receiver>

        <!-- Widget 2: Tasks List Widget -->
        <receiver
            android:name="com.example.widget.TasksListAppWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.widget.ACTION_WIDGET_ITEM_CLICK" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_tasks_list_info" />
        </receiver>

        <!-- Widget 2 RemoteViews Service -->
        <service
            android:name="com.example.widget.TasksWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- Widget 3: Quick Capture Pill Widget -->
        <receiver
            android:name="com.example.widget.QuickCapturePillWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_pill_info" />
        </receiver>

        <!-- File Provider for Backup Sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
